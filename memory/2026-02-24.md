## 2026-02-24

- Se validó prueba real de `nzb-repair` con NZB corrupto: la fase PAR2 reparó correctamente, pero el flujo completo falló al subir por error NewsXS (`482 ... bytes remaining negativo`).
- Se ejecutó reparación local sin upload (parche temporal en copia local de `nzb-repair` con `NZB_REPAIR_SKIP_UPLOAD=1` y `NZB_REPAIR_KEEP_TMP=1`) y se obtuvo MKV reparado válido en:
  - `/home/pulgarcito/.openclaw/workspace/manual-repair/test-20260224/tmp-local/Bienvenido a la jungla (2013).mkv`
- El MKV reparado se copió a Unraid y se verificó reproducción correcta en:
  - `/mnt/user/Descargas/Bienvenido a la jungla (2013).mkv`
- Hallazgo clave acordado: el problema principal apunta a los PAR2 generados por el flujo propio (turbo), no a health en sí, porque con PAR2 externo la reparación fue correcta.
- En EDRmount hubo corrección de rumbo importante:
  - Error inicial del asistente: propuso/implementó NZB separado para PAR2; usuario lo rechazó.
  - Se rectificó a lo pedido: NZB solo de media y PAR2 como archivos separados en `inbox/par2`.
- Commits relevantes en `EDRmount`:
  - `35492be` — quitar enfoque de "PAR2 NZB" y dejar PAR2 como archivos en `inbox/par2`.
  - `244eae1` — `health` repara usando PAR2 local de `/inbox/par2` (misma estructura), mueve MKV a `/inbox/media`, limpia BD import, borra NZB corrupto y PAR2 usados.
  - `f22c420` + tag `v1.107` — bump versión a 1.107.
- Despliegue Unraid realizado con flujo acordado (stop/rm, pull latest, recreate contenedor sin tocar volúmenes persistentes). Imagen activa reportada:
  - `ghcr.io/avogabo/edrmount@sha256:d34151b54d20464b20f340d933662cb99b19cee2dd488c4b23c55c146c797eae`
- Se copiaron por solicitud del usuario los NZB originales a Unraid y luego se borraron al detectar que no eran los correctos para esa fase.
- Estado final de carga manual en inbox:
  - NZB media recibido y copiado a `/mnt/user/Nubes/USENET/inbox/nzb/file_35---2f15b1ab-707a-4b15-a486-18b1bd893a2e.nzb`
  - NZB PAR2 recibido y copiado a `/mnt/user/Nubes/USENET/inbox/par2/file_36---2075fdc9-2cb6-49f3-9025-150b30ac10ba.nzb`
- Incidencia operativa en Unraid: contenedor `edrmount` encontrado en estado `Exited (0)`; se revisaron logs (sin error fatal, último log `EDRmount listening on :1516`) y se reinició con `docker start edrmount`.
- Tras reinicio, servicio volvió a `Up` con puerto `1516` publicado.
- Ajuste de naming pedido por Gaby: el archivo PAR2 recibido por Telegram se había copiado con nombre técnico `file_36---...`; se renombró en Unraid a:
  - `/mnt/user/Nubes/USENET/inbox/par2/bienvenido.par2.nzb`
- Preferencia explícita detectada: evitar renombrados inesperados/confusos en archivos entregados; mantener nombres claros/acordados para pruebas.
- Hito de referencia fijado por Gaby: `main` se reseteó a base estable del commit `0f0c0a4` y se publicó como `v1.100.1` (commit release `b3d615a`, luego trigger CI `d9c3b87`). Esta base queda como punto de retorno obligatorio si futuros cambios rompen streaming.
- Confirmación de campo tras recrear `latest` sobre esa base: pruebas con nuevo archivo (`A todos les gustan los diamantes - 1x04`) funcionando correctamente. Instrucción explícita de Gaby: conservar este estado como referencia para rollback.
