## 2026-03-03 — Nueva referencia operativa (AlfredEDR + reparación NZB/PAR)

- Se valida flujo práctico de reparación con `nzb-repair` cuando el NZB de media está corrupto y el PAR está en un NZB separado: debe fusionarse (media+par) para que `nzb-repair` detecte PAR.
- Integración Alfred en `v1.6.24`:
  - `upload.par.keep_mode: "nzb"` por defecto.
  - Alfred genera PAR, sube PAR como lote separado y conserva `.par.nzb` en árbol de `/inbox/par2/...`.
  - Limpia `.par2` locales cuando el flujo PAR-NZB completa correctamente.
- Estado validado en entorno real Unraid:
  - GitHub: `v1.6.24` (`3c78f30`) en `main`.
  - Deploy: `alfrededr:v1.6.24-nativewebdav`.
- Caso de prueba fuerte (corrupción temprana) y recuperación:
  - Se forzó corrupción de primeros segmentos de un NZB.
  - Reparación con PAR comprobada (par2 repair successful), y se confirmó que el contenido reparado puede reimportarse como nuevo NZB.
- Mantenimiento de BD realizado para limpiar entradas de "TERROR MUTANTE (THE VILLAGE)" cuando se pidió reset de ese caso.

**Decisión de referencia:** tomar esta versión/flujo como baseline de trabajo y diagnóstico a partir de ahora.
